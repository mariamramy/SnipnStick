export type { Folder } from './Folder'
export type { Sticker, StyleSettings, EnhancementSettings } from './Sticker.ts'

//cleaner file for imports, so we can do import { Sticker } from './types' instead of import { Sticker } from './types/Sticker'
